import { SmsSortableTableDirective } from './sms-sortable-table.directive';

describe('SmsSortableTableDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new SmsSortableTableDirective();
  //   expect(directive).toBeTruthy();
  // });
});
